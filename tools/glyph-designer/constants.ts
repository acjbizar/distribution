
export const R = 40.0;
export const DX = 80.0;
export const DY = 80.0;

// Padding ensures the 9px stroke isn't cut off at the viewport boundaries
export const PADDING = 12.0;
export const X0 = R + PADDING;
export const Y0 = R + PADDING;

export const STROKE = 9.0;

export const QUADRANT_NAMES = ['TR', 'BR', 'BL', 'TL'];
